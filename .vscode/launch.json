{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(Windows) Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/nevermore-fms.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [{
                "name": "NEVERMORE_LOG",
                "value": "trace"
            }],
            "console": "integratedTerminal",
            "preLaunchTask": "cargo build"
        }
    ]
}